const {
  create,
  findById,
  find,
  findByIdAndUpdate,
  findByIdAndDelete
} = require('./model');

const Model = require('./Model')
const Schema = require('./Schema')



describe('model class', () => {
  it('creates new document', () => {
  };